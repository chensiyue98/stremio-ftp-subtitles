version: '3.8'

services:
  ftp-subtitles:
    build: .
    ports:
      - "7777:7777"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=7777
      - PUBLIC_URL=http://localhost:7777
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:7777/manifest.json', (res) => { process.exit(res.statusCode === 200 ? 0 : 1); }).on('error', () => process.exit(1));"]
      interval: 30s
      timeout: 10s
      retries: 3
